package com.example.quicknotes.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Material original (la dejo por compatibilidad)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650A4)
val PurpleGrey40 = Color(0xFF625B71)
val Pink40 = Color(0xFF7D5260)

// --- Paleta personalizada pastel ---

// Primarios y secundarios
val BlueDark = Color(0xFF3A6D77)      // Azul lápiz suavizado
val BlueMedium = Color(0xFF5B7D85)    // Medio más apagado
val BlueLight = Color(0xFF8FAFB6)     // Azul claro suave
val Accent = Color(0xFFFFC266)        // Naranja pastel (antes #FFA500)
val WhiteText = Color(0xFFFFFFFF)     // Texto blanco puro

// Fondos y tonos suaves
val SoftYellow = Color(0xFFFFF6CC)    // Amarillo pastel (más suave que #FFF2B5)
val SoftBlue = Color(0xFFE6F0F5)      // Azul pastel clarito
val SoftGray = Color(0xFFFAFAFA)      // Gris muy suave (casi blanco)

// Inputs / bordes
val InputBorder = BlueDark
val InputText = BlueDark

// Estados
val ErrorColor = Color(0xFFE57373)    // Rojo pastel
val SuccessColor = Color(0xFF81C784)  // Verde pastel